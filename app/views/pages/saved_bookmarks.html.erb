<div class="m-2 mt-5">
  <h2 class="d-flex justify-content-center">Your saved articles: </h2>
  <% @bookmarks.reverse_each do |bookmark| %>
      <div class="listed-suggested-article">
          <% prob = bookmark.favoritable.probability.round(2) * 100 %>
          <% prob_value = prob.to_i  %>
          <% prob_value = prob_value * -1 if prob_value < 0 %>
        <div class="icon">
          <% if bookmark.favoritable.fake_news_validation == true %>
            <span>🔴</span>
          <% else %>
            <span>🟢</span>
          <% end %>
        </div>
        <div >
              <h3><%= link_to bookmark.favoritable.title.truncate(60), article_path(bookmark.favoritable)%></h3>
          <% if bookmark.favoritable.fake_news_validation == true %>
              <p> This article spreads <span style="color:#f8312f;"> misinformation</span>.<br>We are <%= prob_value %>% confident on this assessment</p>
            <% else %>
              <p> This article is <span style="color:#00d26a;"> verified</span>.<br>We are <%= prob_value %>% confident on this assessment.</p>
            <% end %>
        </div>
      </div>
    <% end %>
</div>
